let circleRadius = +prompt("What is your circle radius?");
let circleRadiusSquared = Math.pow(circleRadius, 2);
const pi = Math.PI;
let squareCircle = pi * circleRadiusSquared;

console.log(squareCircle);